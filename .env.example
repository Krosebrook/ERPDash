# ─────────────────────────────────────────────────────────────────────────────
# EPB Enterprise Dashboard Pro – Environment Variables
# Copy this file to .env (local dev) or set these in your deployment platform.
# NEVER commit a filled-in .env to version control.
# ─────────────────────────────────────────────────────────────────────────────

# Gemini API Key (required)
# Obtain from https://aistudio.google.com/apikey
# Used by: vite.config.ts at BUILD TIME – the key is baked into the JS bundle.
# ⚠ This key will be visible in the compiled client bundle. Restrict it in
#   Google Cloud Console to your deployed origin (e.g. https://your-app.pages.dev).
GEMINI_API_KEY=your_gemini_api_key_here
